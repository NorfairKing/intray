{-# OPTIONS_GHC -fno-warn-orphans #-}

module Intray.API.Protected.Account
  ( AuthCookie (..),
    AccountInfo (..),
  )
where

import Intray.API.Protected.Account.Types (AccountInfo (..))
import Intray.API.Types (AuthCookie (..))
